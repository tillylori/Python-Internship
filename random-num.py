#I have imported the random library so I can use the randomint() function
import random 

#I'm using randint with the range of 1,000,000
randomnum= random.randint(0,1000000)
print("Your Random Number is:" + str(randomnum))
